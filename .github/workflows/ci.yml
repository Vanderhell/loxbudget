name: ci

on:
  push:
  pull_request:

jobs:
  gcc-host:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x tools/check_banned_symbols.sh tools/footprint_check.sh
      - run: make all
      - run: make test
      - run: cmake -S . -B build_cmake && cmake --build build_cmake
      - run: ./build_cmake/test_microlog_adapter
      - run: make examples
      - run: wc -l < examples/01_bare_metal_minimal/main.c | awk '$1 > 50 { exit 1 }'
      - run: ./tools/footprint_check.sh

  clang-host:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update
      - run: sudo apt-get install -y clang
      - run: chmod +x tools/check_banned_symbols.sh tools/footprint_check.sh
      - run: make CC=clang all
      - run: make CC=clang test

  arm-none-eabi-cross:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update
      - run: sudo apt-get install -y gcc-arm-none-eabi
      - run: chmod +x tools/footprint_check.sh
      - run: make cross
      - run: ./tools/footprint_check.sh tiny build/loxbudget_arm.o

  format-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update
      - run: sudo apt-get install -y clang-format
      - run: clang-format --dry-run -Werror include/loxbudget.h src/loxbudget_core.c src/loxbudget_audit.c src/loxbudget_hal.c src/loxbudget_strings.c src/loxbudget_calibration.c tests/test_header_compiles.c tests/test_v0_1.c tests/test_microlog_adapter.c adapters/microlog/loxbudget_microlog_adapter.c adapters/microlog/loxbudget_microlog_adapter.h examples/01_bare_metal_minimal/main.c examples/02_freertos_mqtt_storm/main.c examples/03_esp_idf_flash_budget/main.c tests/integration/test_flash_burnout.c tests/integration/test_mqtt_storm.c tests/integration/test_ota_blocked.c

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update
      - run: sudo apt-get install -y clang-tidy clang
      - run: cmake -S . -B build_tidy -DCMAKE_C_COMPILER=clang -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
      - run: clang-tidy -p build_tidy include/loxbudget.h src/loxbudget_core.c src/loxbudget_audit.c src/loxbudget_hal.c src/loxbudget_strings.c src/loxbudget_calibration.c tests/test_header_compiles.c tests/test_v0_1.c tests/test_microlog_adapter.c adapters/microlog/loxbudget_microlog_adapter.c examples/01_bare_metal_minimal/main.c

  sanitizers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update
      - run: sudo apt-get install -y clang
      - run: chmod +x tools/check_banned_symbols.sh tools/footprint_check.sh
      - run: make CC=clang CFLAGS="-std=c99 -Wall -Wextra -Werror -Iinclude -fsanitize=address,undefined -fno-omit-frame-pointer -O1 -g" LDFLAGS="-fsanitize=address,undefined" test

  audit-enabled:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x tools/check_banned_symbols.sh tools/footprint_check.sh
      - run: make CC=gcc CFLAGS="-std=c99 -Wall -Wextra -Werror -Iinclude -DLOXBUDGET_ENABLE_AUDIT_TRAIL=1" test

  strings-enabled:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x tools/check_banned_symbols.sh tools/footprint_check.sh
      - run: make CC=gcc CFLAGS="-std=c99 -Wall -Wextra -Werror -Iinclude -DLOXBUDGET_ENABLE_DIAGNOSTIC_STRINGS=1" test

  rate-enabled:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x tools/check_banned_symbols.sh tools/footprint_check.sh
      - run: make CC=gcc CFLAGS="-std=c99 -Wall -Wextra -Werror -Iinclude -DLOXBUDGET_ENABLE_RATE_WINDOWS=1" test

  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x tools/check_banned_symbols.sh tools/footprint_check.sh
      - run: make CC=gcc CFLAGS="-std=c99 -Wall -Wextra -Werror -Iinclude -DLOXBUDGET_ENABLE_RATE_WINDOWS=1 -DLOXBUDGET_ENABLE_AUDIT_TRAIL=1" integration

  token-bucket:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x tools/check_banned_symbols.sh tools/footprint_check.sh
      - run: make CC=gcc CFLAGS="-std=c99 -Wall -Wextra -Werror -Iinclude -DLOXBUDGET_ENABLE_RATE_WINDOWS=1 -DLOXBUDGET_RATE_IMPL=2" test

  calibration-enabled:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x tools/check_banned_symbols.sh tools/footprint_check.sh
      - run: make CC=gcc CFLAGS="-std=c99 -Wall -Wextra -Werror -Iinclude -DLOXBUDGET_ENABLE_CALIBRATION=1" test
